name: archie
services:
  svr:
    # don't create new, default network
    network_mode: 'host'
  db:
    # don't create new, default network
    network_mode: 'host'
  lb-app:
    # don't create new, default network
    network_mode: 'host'
